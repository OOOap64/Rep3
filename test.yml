name: Test Project
on : push
jobs:
  lint:
    runs-on: ubunru-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Install deps
        run: npm ci
      - name: Lint 
        run: npm run lint
